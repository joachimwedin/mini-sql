{
  "name": "@mini-sql/api",
  "version": "0.1.0",
  "description": "TypeScript API for Mini SQL Engine",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "postbuild": "rm -rf dist/wasm && cp -r src/wasm dist/wasm 2>/dev/null || true",
    "dev": "tsc --watch",
    "test": "vitest"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3",
    "vitest": "^1.0.4"
  }
}

